import React from "react";

import "./Hero.scss";

const Hero = (props) => {
    return (
        <section className="hero">
            <section className="hero-caption">
                <h1>Hero section title</h1>
                <p>Hero section lite description</p>
            </section>
        </section>
    );
};

export default Hero;
